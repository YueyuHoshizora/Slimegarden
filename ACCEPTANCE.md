# ACCEPTANCE.md — 驗收機制與結果

## 驗收機制

| 類型 | 方式 |
|---|---|
| 核心邏輯 | `node --test 'tests/**/*.test.js'`（合成、配方成功／失敗安全、晝夜分段離線結算、變異護欄、存檔遷移／匯入往返、轉生） |
| 節奏目標 | `node tools/sim.mjs` 以模擬玩家策略量測 PLAN §2 各時點 |
| 介面與流程 | 本機 `python3 -m http.server` ＋ Chromium 瀏覽器操作，檢查主控台零錯誤；無頭瀏覽器結果不等同實體裝置驗證 |
| RWD | 1920×1080、1280×720、800×1000、390×844、375×667：文件與所有面板 `scrollHeight <= clientHeight`、可互動元件 ≥44×44px |
| PWA | 註冊 SW → 離線模式載入 → 遊玩 → 重新整理，確認進度保留與單一註冊 |

## 結果（2026-09-25）

### 自動化
- ✅ 單元測試 8/8 通過。
- ✅ 節奏模擬：首次合成 3 秒、首隻變異 1 秒、首隻稀有變異 2 分、初見 12 配方 3 分 52 秒、全基礎物種 3 分 52 秒、首次轉生 5 小時 20 分（目標 4～6 小時）。
  - ⚠️ 前四項遠快於 PLAN 上限，符合「之內」但比企劃預期節奏快，待小語決定是否放慢。

### 介面與玩法（瀏覽器實測）
- ✅ 召喚；合成：點選三隻、拖放兩種模式。
- ✅ 配方成功（素材消耗、新物種誕生、配方圖鑑登錄）；配方失敗不消耗素材、掉落廢料、顯示溫柔失敗語；日夜限定配方在錯誤時段溫柔阻擋。
- ✅ 離線結算：22:00→07:00 分夜間／日間兩段，以 8 小時上限計入。
- ✅ 轉生：10 階後顯示原初之泥預估與 6 項升級；轉生重置、購買升級、以原初之泥召喚原初系。
- ✅ 裝飾：以廢料製作、點缸內擺放、收納。
- ✅ 圖鑑：物種／變異／配方分頁 SVG 插畫與剪影、分頁不捲動；PNG 下載現行規格與後續驗證見下方 2026-09-26 同步核對。
- ✅ 存檔：JSON 下載、Base64 複製、貼碼預覽摘要→確認覆寫→自動備份→還原備份。
- ✅ 三語：`?lang=zh-Hant|en|ja`（BCP 47）即時切換不重載、`<html lang>` 同步、上一頁同步、無空白文案；v1 存檔 `zh` 遷移為 `zh-Hant`。
- ✅ 晝夜：06:00／18:00 切換；日間光照與夜間星月、夜間史萊姆縮成一團睡覺（以時區模擬驗證）。
- ✅ CSS／JS 皆以 `?v=<內容雜湊>` 載入（import map 涵蓋所有模組）。
- ✅ RWD：五種驗收視口皆無捲軸、無小於 44px 的可互動元件。
- ✅ 手機版（<600px）：分頁為花園／配方／圖鑑／設定，預設停在花園頁（花園＋召喚、合成、常用功能操作列）；配方頁上方保留花園供點選；花園改為填滿畫布並靠底裁切天空，史萊姆約為原本的 2.3 倍大（320×568、375×667、390×844 實測無溢出）。
- ✅ 字級：小字放大約 20%、內文字重 500，1600×900 以上根字級 18px；最小字級手機 11.2px、桌面 11.8px（原為 8.5px）。三語 × 六種視口（含 320×568）實測無捲軸、無新的元素溢出；僅保留原有的名稱省略號與矮螢幕祝福說明兩行截斷。
- ✅ PWA：當時紀錄預快取 38 項；離線載入、召喚、重新整理後進度保留；SW 單一註冊。（目前清單數量見下方 2026-09-26 核對。）

### 2026-09-26 同步核對
- ✅ 此次之前的 Chromium 瀏覽器驗證：介面維持 `zh-Hant` 時匯入 `en` 全圖鑑存檔，畫面語言未被匯入存檔覆蓋；以 `zh-Hant` 儲存並重新載入後仍維持繁中。實作載入時以網址 `?lang=` 優先；有存檔且網址未指定語言時沿用存檔設定，否則使用本機語言偏好，最終預設 `en`。匯入確認及還原備份後以目前介面語言覆寫存檔語言。
- ✅ 圖鑑匯出現行實作為每列 12 格、2288px 寬、2x 輸出；上述 62 隻全圖鑑繁中／英文產圖與繁中圖片目視檢查已完成。原紀錄「2400px 寬、已檢視 en／ja／zh-Hant」與本次可佐證驗收不符，已於上方修正；日文產圖及英文／日文圖片目視檢查不列為已驗證。
- ✅ 現行 `sw-assets.js` 清單共 60 項，原紀錄的 38 項僅代表當時結果，不是現行預快取數量；本次未重跑離線瀏覽器流程。
- ⚠️ 圖鑑可登錄項目上限依現行資料為 1090（物種 62＋配方 36＋變異 62×13＝806＋階級里程碑 62×3＝186）；`CONFIG.economy.codex.totalEntries` 的 904 是物種、配方與變異的合計，也是轉生完成度的封頂分母，不是包含階級里程碑的全部圖鑑條目。此處只釐清口徑，未改動遊戲數值。
- 此前測試回報失敗數為 0；本次未重新執行遊戲測試、節奏模擬或瀏覽器流程。已執行 `node tools/gen-design.mjs`、產生器語法檢查及重複生成一致性檢查，確認生成文件分別列出 1090 條與 904 分母。上方 2026-09-25 的測試與瀏覽器結果均為歷史紀錄，不等於本次重測。

### 部署資源壓縮驗證
- `npm ci && npm run build && npm test` 成功；核心測試 8/8 通過。僅新增 esbuild 建置相依，原始碼仍可直接由靜態伺服器執行。
- `dist/` 共壓縮 28 個 JS／CSS（包含 `sw.js` 與 `sw-assets.js`），268276 → 217220 bytes，約減少 19.0%；未包含 HTTP gzip／Brotli。
- 逐一核對 60 項預快取資源存在、26 個 CSS／JS 雜湊符合壓縮後內容，import map 的模組網址皆納入快取。
- 本機壓縮產物於 Chromium 1365×768 開啟並目視庭園，召喚後存檔由 1 隻增為 2 隻；離線重新載入保留 2 隻，再離線召喚增為 3 隻。頁面無水平溢出，瀏覽器未回報執行錯誤。
- GitHub Pages 已改為僅上傳 `dist/`；此次未推送或驗證遠端 Actions／正式部署。英文、日文與其他視口未在此輪重測。

### 尚未驗證
- ⬜ 音訊實際聽感：24 首音軌格式已驗證（拍位對齊、音域），先前 Chromium 瀏覽器內已驗證依時段洗牌輪播與回訪玩家首次點擊即解鎖；實際聲音未聽取（無頭環境），Safari／Firefox 實機音訊播放及解鎖也未驗證。
- ⬜ 實機安裝為獨立視窗（iOS／Android 主畫面）。
- ⬜ 正式網址 `https://slimegarden.yustellar.dev` 部署結果（需 GitHub Pages 來源設為 GitHub Actions）。
- ⬜ 三語文案由小語審定（PLAN §7）。
- ⬜ 圖鑑插畫 400% 檢視：SvgArt 階段已驗證樣本，未逐一檢視全部 62 物種。

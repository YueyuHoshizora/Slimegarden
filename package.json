{
  "name": "slimegarden",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tools/build-site.mjs",
    "test": "node --test 'tests/**/*.test.js'"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
